import React, { Component } from 'react';
import './style.css';

class Cell extends Component {
  state = {
    
  };

  render() {
    return (
      <button className="cell"></button>
    );
  }
}

export default Cell;
